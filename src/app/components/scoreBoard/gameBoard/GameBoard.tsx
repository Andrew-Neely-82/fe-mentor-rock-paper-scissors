import React from "react";
// prettier-ignore
interface GameBoardProps { propName: string; }

const GameBoard: React.FC<GameBoardProps> = ({ propName }) => {
  return (
    <div className="">
      <></>
    </div>
  );
};

export default GameBoard;
